{
  "type": "object",
  "javaType": "org.jbpm.serverless.workflow.api.produce.Produce",
  "properties": {
    "eventName": {
      "type": "string",
      "description": "Name of the referenced event",
      "minLength": 1
    },
    "dataRef": {
      "type": "string",
      "description": "Reference to the data element to send with event",
      "minLength": 1
    },
    "metadata": {
      "$ref": "../metadata/metadata.json"
    }
  },
  "required": [
    "eventName"
  ]
}